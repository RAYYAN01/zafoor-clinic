# Postgres connection string. The public website (../website) MUST point
# at this same database — that's how a website booking shows up in the CRM
# instantly, with no sync job. See ../website/.env.example.
DATABASE_URL="postgresql://user:password@localhost:5432/zafoor_clinic"

# Optional — not currently used by the app, reserved for future
# notification integrations mentioned in the project brief.
EMAIL_API_KEY=""
SMS_API_KEY=""
