<%- include('partials/head', { title, description }) %>
<%- include('partials/nav', { clinic }) %>

<main id="main">
  <section class="border-b border-line bg-surface">
    <div class="mx-auto max-w-4xl px-4 py-14 text-center sm:px-6 lg:px-8">
      <p class="section-label">Patient Reviews</p>
      <h1 class="mt-3 text-3xl font-semibold tracking-tight text-dark sm:text-4xl">What patients say</h1>
    </div>
  </section>

  <section class="mx-auto max-w-4xl px-4 py-14 sm:px-6 lg:px-8">
    <% if (reviews.length === 0) { %>
    <p class="text-center text-sm text-muted">Reviews will appear here as patients share their experience.</p>
    <% } else { %>
    <div class="grid gap-5 sm:grid-cols-2">
      <% reviews.forEach(function (r) { %>
      <div class="card p-6">
        <div class="flex items-center gap-1 text-amber-500" aria-label="<%= r.rating %> out of 5 stars">
          <% for (let i = 0; i < 5; i++) { %><span><%= i < r.rating ? "★" : "☆" %></span><% } %>
        </div>
        <p class="mt-3 text-sm leading-relaxed text-ink/90">&ldquo;<%= r.comment %>&rdquo;</p>
        <p class="mt-3 text-sm font-medium text-dark"><%= r.patientName %><% if (r.serviceName) { %> · <span class="text-muted"><%= r.serviceName %></span><% } %></p>
      </div>
      <% }) %>
    </div>
    <% } %>
  </section>

  <section class="border-t border-line bg-surface">
    <div class="mx-auto max-w-4xl px-4 py-14 sm:px-6 lg:px-8">
      <p class="section-label text-center">In Their Own Words</p>
      <h2 class="mt-2 text-center text-2xl font-semibold tracking-tight text-dark sm:text-3xl">Video reviews</h2>
      <div class="mt-8 grid gap-5 sm:grid-cols-2">
        <div class="card overflow-hidden">
          <video class="aspect-[9/16] w-full bg-dark object-cover" controls preload="metadata" playsinline>
            <source src="/videos/video-reviews/skincare-review-1.mp4" type="video/mp4" />
          </video>
          <p class="p-4 text-sm text-muted">Patient video review — skincare treatment</p>
        </div>
        <div class="card overflow-hidden">
          <video class="aspect-[9/16] w-full bg-dark object-cover" controls preload="metadata" playsinline>
            <source src="/videos/video-reviews/irregular-periods-1.mp4" type="video/mp4" />
          </video>
          <p class="p-4 text-sm text-muted">Patient video review — general consultation</p>
        </div>
      </div>
    </div>
  </section>
</main>

<%- include('partials/footer', { clinic }) %>
