# MUST be the exact same connection string as ../crm/.env — the website
# and the CRM share one Postgres database. Do not point this at a
# different database; that would silently break the "website booking
# appears in the CRM immediately" requirement.
DATABASE_URL="postgresql://user:password@localhost:5432/zafoor_clinic"

PORT=3001
NODE_ENV="development"
